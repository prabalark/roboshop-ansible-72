- name: import nodejs
  ansible.builtin.import_role:
    name: common
    tasks_from: java
    #func_nodejs instead of call func here ansible call

    # component=shipping   #in vars/main.yml
    # schma_check=mysql    #in vars/main.yml
    # func_nodejs  # here only
